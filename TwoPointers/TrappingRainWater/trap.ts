function    trap(height : number[]) : number
{
    let res = 0;
    let Lcol = 0;
    let Rcol = 0;
    let left = 0;
    let right = height.length - 1;

    while (left < right)
    {
        if (height[left] < height[right])
        {
            // xu ly left
        }
        else
        {
            // xu ly right
        }
    }
    return res;
}